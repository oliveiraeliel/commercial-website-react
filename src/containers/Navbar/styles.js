import styled from "styled-components";

export const Board = styled.div`
  height: 62px;
  display: flex;
  justify-content: end;
  padding: 2px;
  border-bottom: 2px solid var(--color-turquoise-blue);
`;
